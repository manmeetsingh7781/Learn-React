import React from "react"
import FirstPage from "./FirstPage"

// export default because this is the only function we are exporting
export default function MainContent() {
    return (
    <>
        <FirstPage />
    </>
    )
}